apiVersion: v1
kind: Service
metadata:
  name: token-service
  namespace: fonapp
  labels:
    app: token-app
    environment: dev
    managed-by: argocd
spec:
  selector:
    app: token-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
